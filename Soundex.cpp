#include "Soundex.h"
#include <ctype.h>
#include <string.h>

int isEmpty(const char*inputString)
{
if(strlen(inputString)==0)
  return 1;
}
  
void generateSoundex (const char*inputString, const char*SoundexCode){
if(isEmpty){
  strcpy(Soundex[0] , '\0');
    }


